parameters:
  linux:
    system:
      user:
        bkupidura:
          enabled: false
          name: bkupidura
          sudo: ${_param:linux_system_user_sudo}
          full_name: disabled
          home: /home/bkupidura
          email: disabled
  openssh:
    server:
      user:
        bkupidura:
          enabled: false
          user: ${linux:system:user:bkupidura}
