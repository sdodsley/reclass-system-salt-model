parameters:
  linux:
    system:
      user:
        marco:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/marco
          name: marco
  openssh:
    server:
      user:
        marco:
          enabled: false
          user: ${linux:system:user:marco}
