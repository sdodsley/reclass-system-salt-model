parameters:
  linux:
    system:
      user:
        apetrenko:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/apetrenko
          name: apetrenko
  openssh:
    server:
      user:
        apetrenko:
          enabled: false
          user: ${linux:system:user:apetrenko}
