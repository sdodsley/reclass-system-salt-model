parameters:
  linux:
    system:
      user:
        dlapshin:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/dlapshin
          name: dlapshin
  openssh:
    server:
      user:
        dlapshin:
          enabled: false
          user: ${linux:system:user:dlapshin}
