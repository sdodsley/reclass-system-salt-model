parameters:
  linux:
    system:
      user:
        chnyda:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/chnyda
          name: chnyda
  openssh:
    server:
      user:
        chnyda:
          enabled: false
          user: ${linux:system:user:chnyda}
