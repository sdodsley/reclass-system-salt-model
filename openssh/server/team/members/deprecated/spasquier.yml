parameters:
  linux:
    system:
      user:
        spasquier:
          enabled: false
          name: spasquier
          sudo: ${_param:linux_system_user_sudo}
          full_name: disabled
          home: /home/spasquier
          email: disabled
  openssh:
    server:
      user:
        spasquier:
          enabled: false
          user: ${linux:system:user:spasquier}
