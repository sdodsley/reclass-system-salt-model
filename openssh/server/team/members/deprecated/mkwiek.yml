parameters:
  linux:
    system:
      user:
        mkwiek:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/mkwiek
          name: mkwiek
  openssh:
    server:
      user:
        mkwiek:
          enabled: false
          user: ${linux:system:user:mkwiek}
