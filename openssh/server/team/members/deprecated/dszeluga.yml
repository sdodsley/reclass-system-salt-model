parameters:
  linux:
    system:
      user:
        dszeluga:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/dszeluga
          name: dszeluga
  openssh:
    server:
      user:
        dszeluga:
          enabled: false
          user: ${linux:system:user:dszeluga}
