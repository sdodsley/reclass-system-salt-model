parameters:
  linux:
    system:
      user:
        slagun:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/slagun
          name: slagun
  openssh:
    server:
      user:
        slagun:
          enabled: false
          user: ${linux:system:user:slagun}
