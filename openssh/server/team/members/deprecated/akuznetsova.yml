parameters:
  linux:
    system:
      user:
        akuznetsova:
          enabled: false
          name: akuznetsova
          sudo: ${_param:linux_system_user_sudo}
          full_name: disabled
          home: /home/akuznetsova
          email: disabled
  openssh:
    server:
      user:
        akuznetsova:
          enabled: false
          user: ${linux:system:user:akuznetsova}
