parameters:
  linux:
    system:
      user:
        nkondra:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/nkondra
          name: nkondra
  openssh:
    server:
      user:
        nkondra:
          enabled: false
          user: ${linux:system:user:nkondra}
