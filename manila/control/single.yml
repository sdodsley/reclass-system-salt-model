classes:
 - system.manila.common.cluster
 - system.apache.server.site.manila
parameters:
  manila:
    common:
      dhss: false
      version: ${_param:openstack_version}
    api:
      enabled: true
    scheduler:
      enabled: true
